import pytest
import System

def test_login_system(database):
    user = System.login(self, saab, boomr345)
role = self.users[name]['role']

@pytest.fixture
def database_system():
    database = System.System()
    database.load_data()

    return database